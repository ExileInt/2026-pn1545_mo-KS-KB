<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="#FF313131">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10">
            <Label Content="Podaj liczbę kul:" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24"/>
            <TextBox Name="BallsCountTextBox" Text="{Binding BallsCount}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" Width="200"/>
            <Button Name="StartButton" Content="Start" Width="100" Height="40" Margin="50,0,10,0" Command="{Binding StartCommand}"/>
            <Button Name="StopButton" Content="Stop" Width="100" Height="40" Click="StopButton_Click" Command="{Binding StopCommand}"/>
        </StackPanel>
        <Border BorderBrush="DarkGray" BorderThickness="10" Width="580" Height="300" 
                HorizontalAlignment="Center" VerticalAlignment="Center">

            <ItemsControl ItemsSource="{Binding Balls}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="560" Height="280" Background="#FF155843" ClipToBounds="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="Red"/>
                        
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </Border>
    </Grid>
</Window>
